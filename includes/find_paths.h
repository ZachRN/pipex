#ifndef FIND_PATHS_H
# define FIND_PATHS_H

/*The command given should be something along the lines such as LS BIN etc..
that can then be found in the environment*/
char *find_paths(char *command);

#endif